{
  "name": "@seed-forge/example-testing-vitest",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter verbose",
    "test:determinism": "vitest run --reporter verbose src/determinism.test.ts"
  },
  "dependencies": {
    "@seed-forge/core": "workspace:*",
    "@seed-forge/adapter-postgres": "workspace:*",
    "@seed-forge/testing": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "testcontainers": "^10.9.0",
    "vitest": "^1.6.0",
    "typescript": "^5.5.0"
  }
}
